% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/two_legged_coupling.R
\name{two_legged_coupling}
\alias{two_legged_coupling}
\title{Calculate two-legged matrix for evaluating land-atmosphere coupling strength.}
\usage{
two_legged_coupling(
  land,
  flux,
  atmos,
  sign.level = 0.05,
  parallel = F,
  core.frac = 0.5,
  core.max = 10
)
}
\arguments{
\item{land}{a numeric vector or 3D array representing time series of land status (e.g., soil moisture)}

\item{flux}{a numeric vector or 3D array representing time series of energy fluxes (e.g., latent heat flux)}

\item{atmos}{a numeric vector or 3D array representing time series of atmospheric status (e.g., height of planet boundary height)}

\item{sign.level}{a number 0~1 determining the threshold of correlation significance}

\item{parallel}{TRUE or FALSE (default). if parallel computation will be triggered.}

\item{core.frac}{a number 0~1 controling the maximum cores utilized for parallel computation (fraction of total cores available). It works only when parallel=T. The number of cores are contrainted by both core.frac and cor.max.}

\item{core.max}{a integer (=5 by default) controling the maximum cores utilized for parallel computation. It works only when parallel=T. The number of cores are contrainted by both core.frac and cor.max.}
}
\value{
a list containing (1) the coupling strenghs of land, atmosphere, and total leg; (2) significance of land and atmosphere coupling
}
\description{
Calculate two-legged matrix for evaluating land-atmosphere coupling strength.
}
